
<div class="form-check">
    <input 
        x-model="form.{{name}}"
        type="radio" 
        name="{{name}}" 
        class="form-check-input" 
        :value="'{{value}}'"
>
    <label class="form-check-label" for="{{name}}">
        {{label}}
    </label>
</div>
