<div class="mb-3">
    <label for="{{name}}">{{ label }}</label>
    <textarea name="{{ name || 'content' }}" id="editor" x-model="form.{{name}}"></textarea>
    <div class="text-danger" x-text="errors.{{name}}"></div>
</div>
